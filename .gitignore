# build
dist/
.astro/
node_modules/

# legacy hugo (kept until Task 18 removes the files)
resources/
.hugo_build.lock

# misc
*~
zaucker.tar.gz

# playwright mcp screenshots/logs
.playwright-mcp/

# loose source photo scratch folder (not used by the build)
images/

# calendar service build/secret artifacts
server/node_modules/
server/dist/
server/.cache/
server/config.local.json
